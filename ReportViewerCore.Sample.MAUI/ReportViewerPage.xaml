<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:rv="clr-namespace:Microsoft.ReportViewer.MAUI.Controls;assembly=Microsoft.ReportViewer.MAUI"
             x:Class="ReportViewerCore.Sample.MAUI.ReportViewerPage"
             Title="{Binding ReportTitle}">

    <Grid RowDefinitions="*">
        
        <!-- Report Viewer Control -->
        <rv:ReportViewer x:Name="reportViewer"
                         ShowToolbar="True"
                         ShowExportButton="True"
                         ShowPrintButton="True"
                         ShowRefreshButton="True"
                         RenderingComplete="OnRenderingComplete"
                         ReportError="OnReportError"
                         ExportRequested="OnExportRequested" />
        
        <!-- Loading Indicator -->
        <Grid x:Name="loadingOverlay"
              Background="#80000000"
              IsVisible="False">
            <VerticalStackLayout VerticalOptions="Center"
                                 HorizontalOptions="Center">
                <ActivityIndicator IsRunning="True"
                                   Color="White"
                                   HeightRequest="60"
                                   WidthRequest="60" />
                <Label Text="Loading report..."
                       TextColor="White"
                       FontSize="16"
                       HorizontalOptions="Center"
                       Margin="0,10,0,0" />
            </VerticalStackLayout>
        </Grid>
        
    </Grid>

</ContentPage>
